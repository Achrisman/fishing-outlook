# fishing-outlook

Live intelligence modules for Alex's Bay Area fishing outlook system. Read by Claude sessions at query time (raw URLs), written back via Contents API under the write protocol in OUTLOOK_PROCESS.md. Summary/analysis CSVs are never a valid data basis — raw filtered records only.
